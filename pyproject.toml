[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mosaic-svc-lab"
version = "0.4.0"
description = "Backend-agnostic experiment and retrieval tools for zero-shot singing voice conversion"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "MikanNigata" }]
dependencies = []

[project.optional-dependencies]
audio = ["librosa>=0.10,<0.12", "numpy>=1.24,<3", "soundfile>=0.12"]
visualization = ["matplotlib>=3.7,<4"]

[project.scripts]
mosaic-lab = "mosaic_lab.cli:main"

[tool.setuptools.packages.find]
include = ["mosaic_lab*"]

[tool.unittest]
start-directory = "tests"
